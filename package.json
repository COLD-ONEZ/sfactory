{
  "name": "seriesbot-webapp",
  "version": "1.0.0",
  "description": "Telegram Web App for Series Bot",
  "private": true,
  "scripts": {
    "install:backend": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "dev": "cd backend && npm run dev"
  }
}
